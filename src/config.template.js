export default {
  // The google maps api key
  key: "REPLACE_ME",
  // The distance in pixels above which every .control-point is triggered
  scrollOffset: 300,
  // The delay, in miliseconds, between every potential re-render of content after scroll
  scrollDelay: 350
}
